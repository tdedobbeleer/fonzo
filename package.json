{
  "name": "Fonzo",
  "version": "1.0.0",
  "description": "Fonzo",
  "scripts": {
    "lite": "minify src/js/ui.js && lite-server --port 8080 --baseDir=./src",
    "minify": "minify src/js/ui.js",
    "start": "npm run lite",
    "deploy-test": "minify src/js/ui.js && firebase use fonzo-test && firebase deploy --only hosting && node firebase-cleanup.js fonzo-test 5 commit",
    "deploy": "minify src/js/ui.js && firebase use fonzo-prod && firebase deploy --only hosting && node firebase-cleanup.js fonzo-prod 5 commit"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.4.0npm",
    "firebase-tools": "latest",
    "minifier": "latest"
  }
}
